kind = "web"
previewPath = "/"
title = "OriginLock"
version = "1.0.0"
id = "artifacts/originlock"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20241

[services.development]
run = "pnpm --filter @workspace/originlock run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/originlock", "run", "build" ]
publicDir = "artifacts/originlock/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20241"
BASE_PATH = "/"
